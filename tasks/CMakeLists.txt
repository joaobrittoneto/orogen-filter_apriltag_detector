# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(filter_apriltag_detectorTaskLib)
ADD_LIBRARY(${FILTER_APRILTAG_DETECTOR_TASKLIB_NAME} SHARED 
    ${FILTER_APRILTAG_DETECTOR_TASKLIB_SOURCES})
add_dependencies(${FILTER_APRILTAG_DETECTOR_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${FILTER_APRILTAG_DETECTOR_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${FILTER_APRILTAG_DETECTOR_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${FILTER_APRILTAG_DETECTOR_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${FILTER_APRILTAG_DETECTOR_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${FILTER_APRILTAG_DETECTOR_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${FILTER_APRILTAG_DETECTOR_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${FILTER_APRILTAG_DETECTOR_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${FILTER_APRILTAG_DETECTOR_TASKLIB_HEADERS}
    DESTINATION include/orocos/filter_apriltag_detector)

